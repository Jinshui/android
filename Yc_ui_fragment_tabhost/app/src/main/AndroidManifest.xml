<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yanis.yc_ui_fragment_tabhost"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="14" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:logo="@drawable/logo"
        android:theme="@android:style/Theme.NoTitleBar">
        <!--
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Custom.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity android:name=".PhoneBinderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LatestActivitiesActivity" />
        <activity android:name=".SnapupCouponsActivity" />
        <activity android:name=".SnapCouponDialogActivity" android:theme="@style/Theme.CustomDialog"/>
        <activity android:name=".CouponDetailActivity" />
        <activity android:name=".GesturePwdActivity" />
        <activity android:name=".MyCouponsActivity" />
        <activity android:name=".BarcodeActivity" />
        <activity android:name=".MainActivity" android:theme="@style/Theme.Custom.NoActionBar" />
        <activity android:name=".RechargeActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.Custom.NoActionBar"/>
    </application>

</manifest>
